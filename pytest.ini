[pytest]
# -n auto runs the suite across all CPU cores via pytest-xdist. Use `-n 0` to
# force a serial run (e.g. when debugging).
addopts = -n auto --cov=fasttreeshap --cov-report=term-missing --durations=20
testpaths =
    tests
